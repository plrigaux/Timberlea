{
  "name": "file-server",
  "version": "0.0.0",
  "scripts": {
    "client": "npm --prefix ./client/ start",
    "copyToServe": "cleandir server/client && copyfiles --flat --verbose dist/file-server/** server/client/",
    "build_client": "npm --prefix ./client/ run build",
    "copyClientFiles": "node scripts/copyClientFilesToServer.js",
    "build" : "npm run build_client && npm run copyClientFiles",
    "server": "npm --prefix ./server/ run serve",
    "serve": "concurrently \"npm:client\" \"npm:server\"",
    "postinstall": "(cd server && npm install); (cd client && npm install)"
  },
  "private": true,
  "dependencies": {
  },
  "devDependencies": {
    "concurrently": "^6.4.0",
    "fs-extra": "^10.0.0"
  }
}
